mixed-port: 7890
allow-lan: false
mode: global
log-level: warning

dns: 
  use-hosts: true

hosts: 
  # --- 项目域名 ----
  'webapi2.qmai.cn': '47.110.175.16'

proxies:
- {name: localhost, server: 127.0.0.1, port: 8080}
proxy-groups:
  - name: 🔰 选择节点
    type: select
    proxies:
      - DIRECT
